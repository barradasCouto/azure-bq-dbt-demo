
version: 2

models:
  - name: stg_events
    columns:
      - name: event_id
        tests:
          - unique
          - not_null
      - name: event_name
        tests:
          - accepted_values:
              values:
                - page_view
                - add_to_cart
                - begin_checkout
                - purchase
                - signup